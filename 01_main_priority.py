#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""
=========================================================================
01_main_priority.py - POSSIM Customer Priority Maker メインスクリプト
=========================================================================

【このプログラムの役割】
VHC1～HCU12期の顧客データを処理し、
営業優先度付き顧客リストを作成します。

【処理の流れ】
1. VHC1～5, HCU7～12の11期分を読み込み
2. VHC6期データを差分計算で取得
3. 全12期を統合
4. 氏名正規化
5. 重複削除（最後の期を保持）
6. 都道府県情報をマージ
7. 都道府県を抽出
7.5. 郵便番号から都道府県を補完（新機能）
8. 地域別集計
9. アンケート情報をマージ（氏名＋メール、改善版）
10. 優先順位を付与
11. 電話番号・郵便番号の整形
11.5. 住所の標準化（ローマ字→日本語、都道府県名補完）
12. Excel出力（5シート）

【実行方法】
python 01_main_priority.py

【作成者】
KATSUMI KURAHASHI

【最終更新日】
2025-11-05
=========================================================================
"""

import pandas as pd
import sys
from datetime import datetime
from pathlib import Path
import importlib.util

# 設定ファイルとユーティリティをインポート
# Pythonでは数字で始まるモジュール名は直接インポートできないため、
# importlibを使って動的にインポートします

# 02_config.py をインポート
config_spec = importlib.util.spec_from_file_location("config_02", Path(__file__).parent / "02_config.py")
config_module = importlib.util.module_from_spec(config_spec)
sys.modules["config_02"] = config_module
config_spec.loader.exec_module(config_module)

# 03_data_utils.py をインポート
utils_spec = importlib.util.spec_from_file_location("data_utils_03", Path(__file__).parent / "03_data_utils.py")
utils_module = importlib.util.module_from_spec(utils_spec)
sys.modules["data_utils_03"] = utils_module
utils_spec.loader.exec_module(utils_module)

# インポートした内容を現在の名前空間に展開
# config からすべての定数をインポート
from config_02 import *
# data_utils からすべての関数をインポート
from data_utils_03 import *

# =========================================================================
# 【ステップ1】各期のデータを読み込む（VHC1～5, HCU7～12の11期分）
# =========================================================================

def load_all_periods():
    """
    VHC1～5期とHCU7～12期の11期分のCSVファイルを読み込む

    【やっていること】
    1. 各期のCSVファイルを1つずつ開く
    2. 「受講期」の列を追加（例: VHC1, HCU12）
    3. 「期_番号」の列を追加（例: 1, 12）
    4. すべてのデータを1つのリストにまとめる

    【なぜこうするのか】
    後で「最後に受講した期」を判定するため、期番号が必要です。
    例: 1人がVHC3とHCU10を受講している場合、HCU10（10）が最後の期

    戻り値:
        pd.DataFrame: 11期分のデータ（VHC6を除く）
    """

    print("\n" + "="*70)
    print("【ステップ1】各期のデータを読み込み中...")
    print("="*70)

    all_data = []  # すべてのデータを入れる箱

    # 各期のデータを1つずつ読み込む
    # HUBSPOT_CSV_FILES は 02_config.py で定義されている辞書
    for period, filepath in HUBSPOT_CSV_FILES.items():
        try:
            # CSVファイルを読み込む
            # encoding=CSV_ENCODING は 02_config.py で定義（utf-8-sig）
            df = pd.read_csv(filepath, encoding=CSV_ENCODING)

            # 受講期を追加（例: VHC1）
            df['受講期'] = period

            # 期番号を追加（例: 1）
            # PERIOD_MAPPING は 02_config.py で定義されている辞書
            df['期_番号'] = PERIOD_MAPPING[period]

            # リストに追加
            all_data.append(df)

            print(f"  ✅ {period}: {len(df):,} 件")

        except FileNotFoundError:
            # ファイルが見つからない場合はエラーメッセージを表示して終了
            print(f"  ❌ {period}: ファイルが見つかりません - {filepath}")
            sys.exit(1)

    # すべてのデータを1つにまとめる
    # ignore_index=True で行番号を振り直す
    df_non_vhc6 = pd.concat(all_data, ignore_index=True)

    print(f"\n  📊 11期分の総レコード数: {len(df_non_vhc6):,} 件")

    return df_non_vhc6


# =========================================================================
# 【ステップ2】VHC6期データを差分計算で取得
# =========================================================================

def calculate_vhc6_data(df_non_vhc6):
    """
    VHC6期のデータを差分計算で取得する

    【やっていること】
    1. 全体データ（VHC1～HCU12の全期）を読み込む
    2. 11期分のメールアドレスのセットを作成
    3. 全体データのメールアドレスのセットを作成
    4. 差分計算: 全体 - 11期分 = VHC6期

    【計算式】
    全体データ - (VHC1～5 + HCU7～12) = VHC6期データ

    【なぜこうするのか】
    VHC6期の専用ファイルは不正確な可能性があるため、
    正確なデータを得るために差分計算を使います。

    引数:
        df_non_vhc6 (pd.DataFrame): VHC6を除く11期分のデータ

    戻り値:
        tuple: (VHC6期のデータ, 全体データ)
    """

    print("\n" + "="*70)
    print("【ステップ2】VHC6期データを差分計算で取得")
    print("="*70)

    # 全体データを読み込む（VHC1～HCU12の全期）
    # MASTER_DATA_FILE は 02_config.py で定義されているパス
    df_all = pd.read_csv(MASTER_DATA_FILE, encoding=CSV_ENCODING)
    print(f"  📊 全体データ: {len(df_all):,} 件")

    # 11期分のメールアドレスのセットを作成
    # （小文字に変換・空白削除して正確に比較）
    non_vhc6_emails = set(
        df_non_vhc6['Eメール']
        .dropna()  # 空欄を削除
        .str.lower()  # 小文字に変換
        .str.strip()  # 前後の空白を削除
    )

    # 全体データのメールアドレスのセットを作成
    all_emails = set(
        df_all['Eメール']
        .dropna()
        .str.lower()
        .str.strip()
    )

    # 差分計算: 全体 - 11期分 = VHC6期
    # セット演算を使って、全体から11期分を引く
    vhc6_emails = all_emails - non_vhc6_emails

    print(f"  📊 11期分のメールアドレス数: {len(non_vhc6_emails):,} 件")
    print(f"  📊 全体のメールアドレス数: {len(all_emails):,} 件")
    print(f"  📊 VHC6期のメールアドレス数（差分）: {len(vhc6_emails):,} 件")

    # VHC6期のデータを抽出
    # isin() を使って、vhc6_emails に含まれるメールアドレスだけを抽出
    df_vhc6 = df_all[
        df_all['Eメール']
        .str.lower()
        .str.strip()
        .isin(vhc6_emails)
    ].copy()

    # 受講期と期_番号を設定
    df_vhc6['受講期'] = 'VHC6'
    df_vhc6['期_番号'] = 6

    print(f"\n  ✅ VHC6期（差分計算）: {len(df_vhc6):,} 件")

    return df_vhc6, df_all


# =========================================================================
# 【ステップ3】全12期を統合
# =========================================================================

def merge_all_periods(df_non_vhc6, df_vhc6):
    """
    11期分 + VHC6期 = 12期分のデータを統合する

    【やっていること】
    1. 11期分のデータ（df_non_vhc6）とVHC6期のデータ（df_vhc6）を結合
    2. 1つの大きなデータフレームにまとめる

    【注意】
    この時点では重複があります（1人が複数期に参加している場合）
    重複削除は次のステップで行います。

    引数:
        df_non_vhc6 (pd.DataFrame): VHC6を除く11期分のデータ
        df_vhc6 (pd.DataFrame): VHC6期のデータ

    戻り値:
        pd.DataFrame: 全12期のデータ（重複あり）
    """

    print("\n" + "="*70)
    print("【ステップ3】全12期を統合")
    print("="*70)

    # 11期分 + VHC6期 = 12期分
    # pd.concat() で2つのデータフレームを縦に結合
    df_all_periods = pd.concat([df_non_vhc6, df_vhc6], ignore_index=True)

    print(f"  📊 全12期の総レコード数: {len(df_all_periods):,} 件（重複あり）")

    return df_all_periods


# =========================================================================
# 【ステップ4】氏名正規化とメールアドレスの前処理
# =========================================================================

def preprocess_data(df):
    """
    データの前処理を行う

    【やっていること】
    1. 氏名を作成（姓 + 名）
    2. 氏名を正規化（スペース・記号を削除）
    3. メールアドレスを小文字に統一

    【なぜこうするのか】
    後で重複削除を行うため、統一された形式にする必要があります。
    例: 「最上_輝未子」と「最上 輝未子」を同一人物として認識する

    引数:
        df (pd.DataFrame): 全12期のデータ

    戻り値:
        pd.DataFrame: 前処理後のデータ
    """

    print("\n" + "="*70)
    print("【ステップ4】氏名正規化とメールアドレスの前処理")
    print("="*70)

    # 姓と名を結合して氏名を作成
    df['姓'] = df['姓'].fillna('')  # 空欄は空文字に変換
    df['名'] = df['名'].fillna('')
    df['氏名'] = df['姓'] + df['名']

    # 氏名を正規化（スペース・記号を削除）
    # normalize_name() は 03_data_utils.py で定義されている関数
    df['氏名_key'] = df['氏名'].apply(normalize_name)

    # メールアドレスを小文字に統一
    df['Eメール'] = df['Eメール'].str.lower().str.strip()

    print(f"  ✅ 氏名を作成・正規化しました")
    print(f"  ✅ メールアドレスを小文字に統一しました")

    # 氏名の正規化例を表示
    print("\n  【氏名の正規化例】")
    # 元の氏名と正規化後の氏名が異なるレコードを抽出
    sample_df = df[df['氏名'] != df['氏名_key']].head(5)
    for _, row in sample_df.iterrows():
        print(f"    元の氏名: 「{row['氏名']}」 → 正規化後: 「{row['氏名_key']}」")

    return df


# =========================================================================
# 【ステップ5】重複削除（最後の期を保持）
# =========================================================================

def deduplicate_data(df):
    """
    重複を削除し、最後に受講した期のデータを保持する

    【やっていること】
    1. メールアドレスがあるグループとないグループに分ける
    2. 各グループで期_番号を降順ソート（最後の期が先頭に来る）
    3. メールアドレスまたは氏名で重複削除（最初のレコード=最後の期を保持）
    4. 2つのグループを統合

    【なぜこうするのか】
    1人が複数期に参加している場合、最後に受講した期のデータだけを残します。
    例: VHC3とHCU10を受講している人は、HCU10のデータだけを残す

    引数:
        df (pd.DataFrame): 全12期のデータ（重複あり）

    戻り値:
        pd.DataFrame: 重複削除後のデータ
    """

    print("\n" + "="*70)
    print("【ステップ5】重複削除（最後の期を保持）")
    print("="*70)

    # --- メールアドレスありのグループ ---
    df_with_email = df[df['Eメール'].notna() & (df['Eメール'] != '')].copy()
    print(f"  📊 メールアドレスあり: {len(df_with_email):,} 件")

    # 期_番号を降順ソート（最後の期が先頭に来る）
    # ascending=False で降順（12, 11, 10, ...の順）
    df_with_email_sorted = df_with_email.sort_values('期_番号', ascending=False)

    # メールアドレスで重複削除（最初のレコード=最後の期を保持）
    # keep='first' で最初のレコード（=最後の期）を保持
    df_email_dedup = df_with_email_sorted.drop_duplicates(subset=['Eメール'], keep='first')

    print(f"  ✅ 重複削除後: {len(df_email_dedup):,} 件")

    # --- メールアドレスなしのグループ ---
    df_no_email = df[df['Eメール'].isna() | (df['Eメール'] == '')].copy()
    print(f"\n  📊 メールアドレスなし: {len(df_no_email):,} 件")

    # 期_番号を降順ソート
    df_no_email_sorted = df_no_email.sort_values('期_番号', ascending=False)

    # 氏名で重複削除
    df_no_email_dedup = df_no_email_sorted.drop_duplicates(subset=['氏名_key'], keep='first')

    print(f"  ✅ 重複削除後: {len(df_no_email_dedup):,} 件")

    # 両グループを統合
    df_final = pd.concat([df_email_dedup, df_no_email_dedup], ignore_index=True)

    print(f"\n  📊 最終顧客数: {len(df_final):,} 件")

    # 検証: 最後の期を保持しているか（サンプル5件）
    print("\n  【検証】最後の期を保持しているか（サンプル5件）")
    # メールアドレスがあるレコードから5件をランダムサンプリング
    sample_emails = df_final[df_final['Eメール'].notna()]['Eメール'].sample(min(5, len(df_final)))

    for email in sample_emails:
        # 元のデータから、このメールアドレスのすべてのレコードを取得
        records = df[df['Eメール'] == email]
        # 参加した期のリスト
        periods = sorted(records['期_番号'].tolist())
        # 保持された期
        kept_period = df_final[df_final['Eメール'] == email]['期_番号'].values[0]
        print(f"    {email}: 参加期={periods} → 保持={kept_period}")

    return df_final


# =========================================================================
# 【ステップ6】都道府県情報をマージ
# =========================================================================

def merge_prefecture_data(df_final, df_all):
    """
    マスターデータから都道府県情報をマージする

    【やっていること】
    1. マスターデータ（全体データ）から都道府県情報を取得
    2. メールアドレスをキーにマージ
    3. 都道府県／地域、市区町村、番地の情報を追加

    【なぜこうするのか】
    各期のCSVファイルには都道府県情報が不完全な場合があるため、
    マスターデータから正確な情報を取得します。

    引数:
        df_final (pd.DataFrame): 重複削除後のデータ
        df_all (pd.DataFrame): マスターデータ（全体データ）

    戻り値:
        pd.DataFrame: 都道府県情報がマージされたデータ
    """

    print("\n" + "="*70)
    print("【ステップ6】都道府県情報をマージ")
    print("="*70)

    # マージ用のキーを作成（小文字・空白削除）
    df_final['Eメール_key'] = df_final['Eメール'].str.lower().str.strip()
    df_all['Eメール_key'] = df_all['Eメール'].str.lower().str.strip()

    # 都道府県情報をマージ
    # left: df_final の全レコードを保持
    # how='left': 左側（df_final）のすべてのレコードを保持
    # suffixes: 列名が重複した場合の接尾辞
    df_merged = df_final.merge(
        df_all[['Eメール_key', '都道府県／地域', '市区町村', '番地']],
        on='Eメール_key',
        how='left',
        suffixes=('', '_master')
    )

    print(f"  ✅ 都道府県情報をマージしました")

    return df_merged


# =========================================================================
# 【ステップ7】都道府県を抽出
# =========================================================================

def extract_prefecture_column(df):
    """
    住所から都道府県を抽出する

    【やっていること】
    1. 都道府県／地域の列から都道府県を抽出
    2. 抽出できなかった場合は、市区町村から抽出
    3. それでもない場合は、番地から抽出

    【なぜこうするのか】
    地域別集計を行うため、都道府県を正確に取得する必要があります。

    【例】
    都道府県／地域: "東京都渋谷区..." → 都道府県: "東京都"
    市区町村: "横浜市..." → 都道府県: "神奈川県"（都道府県／地域が空欄の場合）

    引数:
        df (pd.DataFrame): 都道府県情報がマージされたデータ

    戻り値:
        pd.DataFrame: 都道府県が抽出されたデータ
    """

    print("\n" + "="*70)
    print("【ステップ7】都道府県を抽出")
    print("="*70)

    # 都道府県／地域から抽出
    # extract_prefecture() は 03_data_utils.py で定義されている関数
    # PREFECTURES は 02_config.py で定義されているリスト
    df['都道府県'] = df['都道府県／地域'].apply(lambda x: extract_prefecture(x, PREFECTURES))

    # 抽出できなかった場合は、市区町村から抽出
    mask_no_pref = df['都道府県'].isna()  # 都道府県がNoneのレコード
    df.loc[mask_no_pref, '都道府県'] = df.loc[mask_no_pref, '市区町村'].apply(lambda x: extract_prefecture(x, PREFECTURES))

    # それでもない場合は、番地から抽出
    mask_no_pref = df['都道府県'].isna()
    df.loc[mask_no_pref, '都道府県'] = df.loc[mask_no_pref, '番地'].apply(lambda x: extract_prefecture(x, PREFECTURES))

    # 都道府県が取得できた件数
    pref_count = df['都道府県'].notna().sum()
    pref_rate = pref_count / len(df) * 100

    print(f"  ✅ 都道府県データ件数: {pref_count:,} 件 / {len(df):,} 件 ({pref_rate:.1f}%)")

    # 都道府県別の人数（上位10件）
    print("\n  【都道府県別の人数（上位10件）】")
    top_prefs = df['都道府県'].value_counts().head(10)
    for i, (pref, count) in enumerate(top_prefs.items(), 1):
        print(f"    {i}. {pref}: {count:,} 人")

    return df


# =========================================================================
# 【ステップ7.5】郵便番号から都道府県を補完
# =========================================================================

def fill_prefecture_from_postal(df):
    """
    郵便番号をもとに都道府県を補完する

    【やっていること】
    1. 郵便番号が存在するが都道府県が空欄のレコードを抽出
    2. 郵便番号の最初の3桁から都道府県を判定
    3. 都道府県を自動補完
    4. 補完結果をログ出力

    【なぜこうするのか】
    都道府県データの補完率を向上させることで、
    地域別集計の精度が上がり、営業戦略に役立ちます。

    引数:
        df (pd.DataFrame): 都道府県が抽出されたデータ

    戻り値:
        pd.DataFrame: 都道府県が補完されたデータ
    """

    print("\n" + "="*70)
    print("【ステップ7.5】郵便番号から都道府県を補完中")
    print("="*70)

    # 補完前の都道府県データ件数
    before_count = df['都道府県'].notna().sum()
    before_rate = before_count / len(df) * 100

    print(f"\n補完前:")
    print(f"  ✅ 都道府県データ件数: {before_count:,} 件 / {len(df):,} 件 ({before_rate:.1f}%)")

    # fill_prefecture_from_postal_code() は 03_data_utils.py で定義されている関数
    # 郵便番号が「郵便番号」列にある場合は、format_postal()で整形済みの「郵便番号_フォーマット」を使う
    # ただし、このステップはステップ11（整形）の前なので、「郵便番号」列を使う
    df, completed_count, failed_count = fill_prefecture_from_postal_code(df, postal_code_col='郵便番号', prefecture_col='都道府県')

    # 補完後の都道府県データ件数
    after_count = df['都道府県'].notna().sum()
    after_rate = after_count / len(df) * 100

    print(f"\n補完後:")
    print(f"  ✅ 都道府県データ件数: {after_count:,} 件 / {len(df):,} 件 ({after_rate:.1f}%)")

    if completed_count > 0:
        print(f"  📊 補完成功: {completed_count:,} 件")
        print(f"     例: 1001234 → 東京都")
        print(f"         5301234 → 大阪府")
        print(f"         9601234 → 福島県")

    if failed_count > 0:
        print(f"  ⚠️  補完失敗（郵便番号が対応範囲外）: {failed_count:,} 件")

    # 補完できなかった残りの件数
    remaining_empty = len(df) - after_count
    print(f"  📍 都道府県が空欄のまま: {remaining_empty:,} 件")

    return df


# =========================================================================
# 【ステップ8】地域別集計
# =========================================================================

def regional_summary(df):
    """
    地域別の集計を行う

    【集計項目】
    A. 東京都在住
    B. 埼玉・千葉・神奈川在住
    C. 7期以降 + 東京都在住
    D. 7期以降 + 近県在住

    【なぜこれらの集計が必要か】
    東京都とその近隣県は、営業活動の優先エリアです。
    特に7期以降（最近の受講生）の人数を把握することで、
    営業計画を立てやすくなります。

    引数:
        df (pd.DataFrame): 都道府県が抽出・補完されたデータ

    戻り値:
        dict: 地域別集計結果
    """

    print("\n" + "="*70)
    print("【ステップ8】地域別集計")
    print("="*70)

    # 集計対象の確認
    total_customers = len(df)
    customers_with_pref = df['都道府県'].notna().sum()
    customers_without_pref = total_customers - customers_with_pref

    print(f"\n  【集計対象】")
    print(f"  全顧客数: {total_customers:,} 人")
    print(f"  都道府県が存在: {customers_with_pref:,} 人 ({customers_with_pref/total_customers*100:.1f}%)")
    print(f"  都道府県が不明: {customers_without_pref:,} 人 ({customers_without_pref/total_customers*100:.1f}%)")

    # A. 東京都在住
    A = (df['都道府県'] == '東京都').sum()

    # B. 埼玉・千葉・神奈川在住
    # KANTO_NEARBY は 02_config.py で定義されているリスト
    B = df['都道府県'].isin(KANTO_NEARBY).sum()

    # C. 7期以降 + 東京都在住
    C = ((df['期_番号'] >= PRIORITY_2_PERIOD_MIN) & (df['都道府県'] == '東京都')).sum()

    # D. 7期以降 + 近県在住
    D = ((df['期_番号'] >= PRIORITY_2_PERIOD_MIN) &
         (df['都道府県'].isin(KANTO_NEARBY))).sum()

    print(f"\n  【地域別集計結果】")
    print(f"  A. 東京都在住: {A:,} 人")
    print(f"  B. 埼玉・千葉・神奈川在住: {B:,} 人")
    print(f"  C. 7期以降 + 東京都在住: {C:,} 人")
    print(f"  D. 7期以降 + 近県在住: {D:,} 人")

    # 合計チェック
    total_regional = A + B
    print(f"\n  📊 東京都+近県の合計: {total_regional:,} 人（全顧客の{total_regional/total_customers*100:.1f}%）")

    return {
        'A': A,
        'B': B,
        'C': C,
        'D': D,
    }


# =========================================================================
# 【ステップ9】アンケート情報をマージ
# =========================================================================

def merge_survey_data(df):
    """
    卒業式アンケートの回答情報をマージする（氏名＋メールアドレス）

    【やっていること】
    1. 7～11期の卒業式アンケートファイルを読み込む
    2. 回答者の氏名とメールアドレスを取得
    3. 氏名で紐づけを試行、失敗したらメールアドレスで試行
    4. 回答者にフラグを立てる（True/False）
    5. アンケート回答詳細データを生成

    【改善内容】
    - 氏名での紐づけを第1段階として実行
    - メールアドレスでの紐づけを第2段階として実行
    - マッチング結果を詳細にログ出力
    - Excel出力用の詳細データを生成

    【なぜこうするのか】
    優先順位2の判定に必要です（7～11期 + アンケート回答者）。
    アンケート回答者は、POSSIMとの関係性が強いと判断します。

    引数:
        df (pd.DataFrame): 都道府県が抽出されたデータ

    戻り値:
        tuple: (アンケート回答フラグが追加されたデータ, アンケート回答者のメールアドレスセット, アンケート回答詳細データ)
    """

    print("\n" + "="*70)
    print("【ステップ9】アンケート情報をマージ")
    print("="*70)

    # merge_survey_data_enhanced() は 03_data_utils.py で定義されている関数
    # SURVEY_FILES は 02_config.py で定義されている辞書
    df, survey_emails = merge_survey_data_enhanced(df, SURVEY_FILES)

    # アンケート回答詳細データを生成
    print("\n  【アンケート回答詳細データを生成中...】")
    df_survey_details = generate_survey_response_details(df, SURVEY_FILES)
    print(f"  ✅ アンケート回答詳細データ: {len(df_survey_details):,} 件")

    return df, survey_emails, df_survey_details


# =========================================================================
# 【ステップ10】優先順位を付与
# =========================================================================

def assign_priority(df, survey_emails):
    """
    優先順位を計算して付与する

    【定義】
    優先順位1: 最後に参加したのが12期
    優先順位2: 7～11期 かつ アンケート回答者
    優先順位3: 7～11期 かつ 未回答
    優先順位4: 1～6期

    【なぜこの順番なのか】
    - 12期生は最新の受講生なので、最も優先度が高い
    - 7～11期でアンケート回答者は、関係性が強いと判断
    - 1～6期は受講から時間が経っているため、優先度は低い

    引数:
        df (pd.DataFrame): アンケート回答フラグが追加されたデータ
        survey_emails (set): アンケート回答者のメールアドレスセット

    戻り値:
        pd.DataFrame: 優先順位が追加されたデータ
    """

    print("\n" + "="*70)
    print("【ステップ10】優先順位を付与")
    print("="*70)

    # 優先順位を計算
    # calculate_priority() は 03_data_utils.py で定義されている関数
    df['優先順位'] = df.apply(lambda row: calculate_priority(row, survey_emails), axis=1)

    # 優先順位別の人数
    priority_counts = df['優先順位'].value_counts().sort_index()

    print("\n  【優先順位別の人数】")
    # PRIORITY_LABELS は 02_config.py で定義されている辞書
    for priority in [1, 2, 3, 4]:
        count = priority_counts.get(priority, 0)
        print(f"    {PRIORITY_LABELS[priority]}: {count:,} 人")

    print(f"    合計: {len(df):,} 人")

    return df


# =========================================================================
# 【ステップ11】電話番号・郵便番号の整形と住所の作成
# =========================================================================

def format_contact_info(df):
    """
    電話番号・郵便番号を整形し、住所を作成する

    【やっていること】
    1. 電話番号を整形（090-1234-5678形式）
    2. 郵便番号を整形（123-4567形式）
    3. 住所を作成（都道府県 + 市区町村 + 番地）

    【なぜこうするのか】
    見やすく統一された形式にすることで、
    営業活動で使いやすくなります。

    引数:
        df (pd.DataFrame): 優先順位が追加されたデータ

    戻り値:
        pd.DataFrame: 整形後のデータ
    """

    print("\n" + "="*70)
    print("【ステップ11】電話番号・郵便番号の整形と住所の作成")
    print("="*70)

    # 電話番号を整形
    # format_phone() は 03_data_utils.py で定義されている関数
    df['電話番号_フォーマット'] = df['電話番号'].apply(format_phone)

    # 郵便番号を整形
    # format_postal() は 03_data_utils.py で定義されている関数
    df['郵便番号_フォーマット'] = df['郵便番号'].apply(format_postal)

    # 住所を作成
    df['住所'] = (
        df['都道府県／地域'].fillna('') +
        df['市区町村'].fillna('') +
        df['番地'].fillna('')
    )

    # データ品質チェック
    phone_count = df['電話番号'].notna().sum()
    phone_rate = phone_count / len(df) * 100

    postal_count = df['郵便番号'].notna().sum()
    postal_rate = postal_count / len(df) * 100

    address_count = df['住所'].notna().sum()
    address_rate = address_count / len(df) * 100

    email_count = df['Eメール'].notna().sum()
    email_rate = email_count / len(df) * 100

    print(f"  ✅ 電話番号データ件数: {phone_count:,} 件 / {len(df):,} 件 ({phone_rate:.1f}%)")
    print(f"  ✅ 郵便番号データ件数: {postal_count:,} 件 / {len(df):,} 件 ({postal_rate:.1f}%)")
    print(f"  ✅ 住所データ件数: {address_count:,} 件 / {len(df):,} 件 ({address_rate:.1f}%)")
    print(f"  ✅ メールアドレスデータ件数: {email_count:,} 件 / {len(df):,} 件 ({email_rate:.1f}%)")

    # 住所の重複チェック
    print("\n  【住所の重複チェック】")
    address_counts = df['住所'].value_counts()
    duplicates = address_counts[address_counts > 1]

    if len(duplicates) > 0:
        print(f"  ⚠️ 重複住所が {len(duplicates):,} 件見つかりました")
        print("\n  上位 10 件:")
        for i, (address, count) in enumerate(duplicates.head(10).items(), 1):
            display_address = address if address else '（空欄）'
            print(f"    {i}. {display_address}: {count:,} 件")
    else:
        print(f"  ✅ 重複住所はありません")

    return df


# =========================================================================
# 【ステップ11.5】住所の標準化
# =========================================================================

def standardize_addresses(df):
    """
    住所を標準化する（ローマ字→日本語、都道府県名補完）

    【やっていること】
    1. ローマ字住所を日本語に変換
    2. 都道府県名から始まる住所に標準化
    3. 標準化された住所列を追加

    【なぜこうするのか】
    住所データの品質を向上させることで、
    営業活動で使いやすくなります。

    引数:
        df (pd.DataFrame): 整形後のデータ

    戻り値:
        pd.DataFrame: 住所標準化後のデータ
    """

    print("\n" + "="*70)
    print("【ステップ11.5】住所の標準化")
    print("="*70)

    # 住所列のコピーを作成
    df['住所_元'] = df['住所'].copy()

    # 1. ローマ字住所を日本語に変換
    print("\n  【ローマ字住所を日本語に変換中...】")
    df['住所'] = df['住所'].apply(convert_romaji_address_to_japanese)

    # 変換された件数をカウント
    romaji_converted = (df['住所'] != df['住所_元']).sum()
    if romaji_converted > 0:
        print(f"  ✅ ローマ字→日本語変換: {romaji_converted:,} 件")
        # サンプルを表示
        sample_df = df[df['住所'] != df['住所_元']].head(3)
        for _, row in sample_df.iterrows():
            print(f"     例: {row['住所_元']} → {row['住所']}")
    else:
        print(f"  ℹ️  ローマ字住所は検出されませんでした")

    # 2. 都道府県名から始まる住所に標準化
    print("\n  【都道府県名を補完中...】")
    df['住所（標準化）'] = df.apply(
        lambda row: standardize_address_with_prefecture(row['住所'], row['都道府県']),
        axis=1
    )

    # 標準化された件数をカウント
    standardized = (df['住所（標準化）'] != df['住所']).sum()
    if standardized > 0:
        print(f"  ✅ 都道府県名補完: {standardized:,} 件")
        # サンプルを表示
        sample_df = df[df['住所（標準化）'] != df['住所']].head(3)
        for _, row in sample_df.iterrows():
            addr_before = row['住所'] if len(str(row['住所'])) < 50 else str(row['住所'])[:50] + '...'
            addr_after = row['住所（標準化）'] if len(str(row['住所（標準化）'])) < 50 else str(row['住所（標準化）'])[:50] + '...'
            print(f"     例: {addr_before}")
            print(f"      → {addr_after}")
    else:
        print(f"  ℹ️  全ての住所が既に都道府県名から始まっています")

    # 3. 郵便番号から都道府県を参照（補完用）
    print("\n  【郵便番号参照住所を作成中...】")
    df['住所（郵便番号参照）'] = df['郵便番号'].apply(
        lambda postal: get_prefecture_from_postal_code(postal, POSTAL_CODE_TO_PREFECTURE) if pd.notna(postal) else None
    )

    # 郵便番号参照で都道府県が取得できた件数
    postal_ref_count = df['住所（郵便番号参照）'].notna().sum()
    print(f"  ✅ 郵便番号参照住所: {postal_ref_count:,} 件")

    # 元の住所列は削除
    df.drop(columns=['住所_元'], inplace=True)

    # データ品質チェック
    address_with_pref_count = df['住所（標準化）'].notna().sum()
    address_with_pref_rate = address_with_pref_count / len(df) * 100

    print(f"\n  【住所データ品質】")
    print(f"  住所（標準化）データ件数: {address_with_pref_count:,} 件 / {len(df):,} 件 ({address_with_pref_rate:.1f}%)")
    print(f"  郵便番号参照住所データ件数: {postal_ref_count:,} 件 / {len(df):,} 件 ({postal_ref_count/len(df)*100:.1f}%)")

    return df


# =========================================================================
# 【ステップ12】Excel出力（5シート構成）
# =========================================================================

def create_excel_output(df, regional_stats, df_survey_details):
    """
    Excelファイルを出力する（5シート構成）

    【シート構成】
    1. 営業リスト: 優先順位付き顧客リスト
    2. 地域別集計結果: 地域別の人数集計
    3. 優先順位別の人数: 優先順位別の人数集計
    4. 各種チェック項目: データ品質チェック結果
    5. アンケート回答詳細: アンケート回答者のマッチング詳細

    【なぜ5シートに分けるのか】
    - シート1: 実際に営業で使うリスト
    - シート2: 地域戦略を立てるための集計
    - シート3: 優先順位別の戦略を立てるための集計
    - シート4: データ品質を確認するためのチェック
    - シート5: アンケート回答者のマッチング状況を確認

    引数:
        df (pd.DataFrame): 整形後のデータ
        regional_stats (dict): 地域別集計結果
        df_survey_details (pd.DataFrame): アンケート回答詳細データ
    """

    print("\n" + "="*70)
    print("【ステップ12】Excel出力（5シート構成）")
    print("="*70)

    # 出力用のデータフレームを作成
    df_output = df[[
        '優先順位',
        '氏名',
        '電話番号_フォーマット',
        'Eメール',
        '郵便番号_フォーマット',
        '住所',
        '住所（標準化）',
        '住所（郵便番号参照）',
        '都道府県',
        '受講期',
        'アンケート回答',
    ]].copy()

    # 列名を変更
    df_output.columns = [
        '優先順位',
        '氏名',
        '電話番号',
        'メールアドレス',
        '郵便番号',
        '住所',
        '住所（標準化）',
        '住所（郵便番号参照）',
        '都道府県',
        '最後に受講した期',
        'アンケート回答',
    ]

    # 優先順位でソート
    df_output = df_output.sort_values('優先順位')

    # Excelファイルを作成
    # OUTPUT_FILE は 02_config.py で定義されているパス
    # EXCEL_ENGINE は 02_config.py で定義されている設定
    with pd.ExcelWriter(OUTPUT_FILE, engine=EXCEL_ENGINE) as writer:

        # --- シート1: 営業リスト ---
        print("  📄 営業リストシートを作成中...")
        # OUTPUT_SHEETS は 02_config.py で定義されている辞書
        df_output.to_excel(writer, sheet_name=OUTPUT_SHEETS['MAIN'], index=False)

        # --- シート2: 地域別集計結果 ---
        print("  📄 地域別集計結果シートを作成中...")
        df_regional = pd.DataFrame([
            {'集計項目': 'A. 東京都在住', '人数': regional_stats['A']},
            {'集計項目': 'B. 埼玉・千葉・神奈川在住', '人数': regional_stats['B']},
            {'集計項目': 'C. 7期以降 + 東京都在住', '人数': regional_stats['C']},
            {'集計項目': 'D. 7期以降 + 近県在住', '人数': regional_stats['D']},
        ])
        df_regional.to_excel(writer, sheet_name=OUTPUT_SHEETS['REGIONAL'], index=False)

        # --- シート3: 優先順位別の人数 ---
        print("  📄 優先順位別の人数シートを作成中...")
        priority_counts = df['優先順位'].value_counts().sort_index()
        df_priority = pd.DataFrame([
            {'優先順位': PRIORITY_LABELS[1], '人数': priority_counts.get(1, 0)},
            {'優先順位': PRIORITY_LABELS[2], '人数': priority_counts.get(2, 0)},
            {'優先順位': PRIORITY_LABELS[3], '人数': priority_counts.get(3, 0)},
            {'優先順位': PRIORITY_LABELS[4], '人数': priority_counts.get(4, 0)},
            {'優先順位': '合計', '人数': len(df)},
        ])
        df_priority.to_excel(writer, sheet_name=OUTPUT_SHEETS['PRIORITY'], index=False)

        # --- シート4: 各種チェック項目 ---
        print("  📄 各種チェック項目シートを作成中...")

        # 基本データの存在チェック
        phone_count = df['電話番号'].notna().sum()
        phone_blank = len(df) - phone_count
        phone_rate = phone_count / len(df) * 100

        postal_count = df['郵便番号'].notna().sum()
        postal_blank = len(df) - postal_count
        postal_rate = postal_count / len(df) * 100

        address_count = df['住所'].notna().sum()
        address_blank = len(df) - address_count
        address_rate = address_count / len(df) * 100

        pref_count = df['都道府県'].notna().sum()
        pref_blank = len(df) - pref_count
        pref_rate = pref_count / len(df) * 100

        email_count = df['Eメール'].notna().sum()
        email_blank = len(df) - email_count
        email_rate = email_count / len(df) * 100

        name_count = df['氏名'].notna().sum()
        name_blank = len(df) - name_count
        name_rate = name_count / len(df) * 100

        # 住所標準化チェック
        standardized_count = df['住所（標準化）'].notna().sum()
        standardized_rate = standardized_count / len(df) * 100

        postal_ref_count = df['住所（郵便番号参照）'].notna().sum()
        postal_ref_rate = postal_ref_count / len(df) * 100

        # ローマ字住所検出（簡易検出：英数字が連続している住所）
        romaji_pattern = r'[A-Za-z]{3,}'
        romaji_address_count = df['住所'].fillna('').str.contains(romaji_pattern, regex=True).sum()

        # メールアドレス重複チェック
        email_duplicates = df[df['Eメール'].notna()]['Eメール'].value_counts()
        email_dup_count = (email_duplicates > 1).sum()

        # 電話番号重複チェック
        phone_duplicates = df[df['電話番号'].notna()]['電話番号'].value_counts()
        phone_dup_count = (phone_duplicates > 1).sum()

        # アンケート回答者数
        survey_response_count = df['アンケート回答'].sum()
        survey_response_rate = survey_response_count / len(df) * 100

        # チェック項目リストを作成
        check_items = [
            # 基本データ存在チェック
            {'カテゴリ': '基本データ', 'チェック項目': '氏名データ件数', '件数': name_count, '空欄': name_blank, '全体': len(df), '割合(%)': f'{name_rate:.1f}'},
            {'カテゴリ': '基本データ', 'チェック項目': 'メールアドレスデータ件数', '件数': email_count, '空欄': email_blank, '全体': len(df), '割合(%)': f'{email_rate:.1f}'},
            {'カテゴリ': '基本データ', 'チェック項目': '電話番号データ件数', '件数': phone_count, '空欄': phone_blank, '全体': len(df), '割合(%)': f'{phone_rate:.1f}'},
            {'カテゴリ': '基本データ', 'チェック項目': '郵便番号データ件数', '件数': postal_count, '空欄': postal_blank, '全体': len(df), '割合(%)': f'{postal_rate:.1f}'},
            {'カテゴリ': '基本データ', 'チェック項目': '住所データ件数', '件数': address_count, '空欄': address_blank, '全体': len(df), '割合(%)': f'{address_rate:.1f}'},
            {'カテゴリ': '基本データ', 'チェック項目': '都道府県データ件数', '件数': pref_count, '空欄': pref_blank, '全体': len(df), '割合(%)': f'{pref_rate:.1f}'},

            # 住所品質チェック
            {'カテゴリ': '住所品質', 'チェック項目': '住所（標準化）データ件数', '件数': standardized_count, '空欄': len(df) - standardized_count, '全体': len(df), '割合(%)': f'{standardized_rate:.1f}'},
            {'カテゴリ': '住所品質', 'チェック項目': '住所（郵便番号参照）データ件数', '件数': postal_ref_count, '空欄': len(df) - postal_ref_count, '全体': len(df), '割合(%)': f'{postal_ref_rate:.1f}'},
            {'カテゴリ': '住所品質', 'チェック項目': 'ローマ字住所検出件数', '件数': romaji_address_count, '空欄': '-', '全体': len(df), '割合(%)': f'{romaji_address_count/len(df)*100:.1f}'},

            # 重複チェック
            {'カテゴリ': '重複チェック', 'チェック項目': 'メールアドレス重複', '件数': email_dup_count, '空欄': '-', '全体': email_count, '割合(%)': f'{email_dup_count/email_count*100:.1f}' if email_count > 0 else '0.0'},
            {'カテゴリ': '重複チェック', 'チェック項目': '電話番号重複', '件数': phone_dup_count, '空欄': '-', '全体': phone_count, '割合(%)': f'{phone_dup_count/phone_count*100:.1f}' if phone_count > 0 else '0.0'},

            # アンケート回答
            {'カテゴリ': 'アンケート', 'チェック項目': 'アンケート回答者数', '件数': survey_response_count, '空欄': len(df) - survey_response_count, '全体': len(df), '割合(%)': f'{survey_response_rate:.1f}'},
        ]

        df_check = pd.DataFrame(check_items)
        df_check.to_excel(writer, sheet_name=OUTPUT_SHEETS['CHECK'], index=False)

        # 住所の重複情報を追加
        address_counts = df['住所'].value_counts()
        duplicates = address_counts[address_counts > 1]

        if len(duplicates) > 0:
            # 空行を追加（新しいカラム数5に合わせる）
            df_empty = pd.DataFrame([[''] * 5], columns=['カテゴリ', 'チェック項目', '件数', '空欄', '全体'])
            df_empty.to_excel(writer, sheet_name=OUTPUT_SHEETS['CHECK'],
                            startrow=len(df_check)+2, index=False, header=False)

            # 重複住所のタイトル
            df_dup_title = pd.DataFrame([['住所重複', '【住所の重複リスト】', '', '', '']],
                                       columns=['カテゴリ', 'チェック項目', '件数', '空欄', '全体'])
            df_dup_title.to_excel(writer, sheet_name=OUTPUT_SHEETS['CHECK'],
                                 startrow=len(df_check)+3, index=False, header=False)

            # 重複住所の情報
            df_dup_info = pd.DataFrame([['住所重複', f'⚠️ 重複住所が {len(duplicates):,} 件見つかりました', '', '', '']],
                                      columns=['カテゴリ', 'チェック項目', '件数', '空欄', '全体'])
            df_dup_info.to_excel(writer, sheet_name=OUTPUT_SHEETS['CHECK'],
                                startrow=len(df_check)+4, index=False, header=False)

            # 空行
            df_empty.to_excel(writer, sheet_name=OUTPUT_SHEETS['CHECK'],
                            startrow=len(df_check)+5, index=False, header=False)

            # 重複住所の上位10件
            df_dup_list = pd.DataFrame([
                [address if address else '（空欄）', count]
                for address, count in duplicates.head(10).items()
            ], columns=['住所', '件数'])
            df_dup_list.to_excel(writer, sheet_name=OUTPUT_SHEETS['CHECK'],
                                startrow=len(df_check)+6, index=False)

        # --- シート5: アンケート回答詳細 ---
        print("  📄 アンケート回答詳細シートを作成中...")
        if len(df_survey_details) > 0:
            df_survey_details.to_excel(writer, sheet_name='アンケート回答詳細', index=False)
        else:
            # アンケート回答者がいない場合は空シートを作成
            pd.DataFrame(columns=[
                '氏名（顧客）', 'メールアドレス（顧客）', '氏名（アンケート）',
                'メールアドレス（アンケート）', 'マッチング方法', 'アンケート回答期',
                'アンケート回答フラグ'
            ]).to_excel(writer, sheet_name='アンケート回答詳細', index=False)

    print(f"\n  ✅ 保存完了: {OUTPUT_FILE}")
    print(f"  📊 シート数: 5 シート")
    print(f"  📊 営業リスト件数: {len(df_output):,} 件")
    print(f"  📊 アンケート回答詳細件数: {len(df_survey_details):,} 件")


# =========================================================================
# 【メイン処理】
# =========================================================================

def main():
    """
    メイン処理

    【処理の流れ】
    1. 各期のデータを読み込む（VHC1～5, HCU7～12の11期分）
    2. VHC6期データを差分計算で取得
    3. 全12期を統合
    4. 氏名正規化とメールアドレスの前処理
    5. 重複削除（最後の期を保持）
    6. 都道府県情報をマージ
    7. 都道府県を抽出
    7.5. 郵便番号から都道府県を補完（新機能）
    8. 地域別集計
    9. アンケート情報をマージ（氏名＋メールアドレス、改善版）
    10. 優先順位を付与
    11. 電話番号・郵便番号の整形と住所の作成
    11.5. 住所の標準化（ローマ字→日本語、都道府県名補完）
    12. Excel出力（5シート構成）
    """

    print("\n" + "="*70)
    print("POSSIM Customer Priority Maker - 実行開始")
    print("="*70)
    print(f"実行日時: {datetime.now().strftime('%Y-%m-%d %H:%M:%S')}")

    try:
        # ステップ1: 各期のデータを読み込む
        df_non_vhc6 = load_all_periods()

        # ステップ2: VHC6期データを差分計算で取得
        df_vhc6, df_all = calculate_vhc6_data(df_non_vhc6)

        # ステップ3: 全12期を統合
        df_all_periods = merge_all_periods(df_non_vhc6, df_vhc6)

        # ステップ4: 氏名正規化とメールアドレスの前処理
        df_preprocessed = preprocess_data(df_all_periods)

        # ステップ5: 重複削除（最後の期を保持）
        df_dedup = deduplicate_data(df_preprocessed)

        # ステップ6: 都道府県情報をマージ
        df_merged = merge_prefecture_data(df_dedup, df_all)

        # ステップ7: 都道府県を抽出
        df_with_pref = extract_prefecture_column(df_merged)

        # ステップ7.5: 郵便番号から都道府県を補完
        df_with_pref_completed = fill_prefecture_from_postal(df_with_pref)

        # ステップ8: 地域別集計
        regional_stats = regional_summary(df_with_pref_completed)

        # ステップ9: アンケート情報をマージ
        df_with_survey, survey_emails, df_survey_details = merge_survey_data(df_with_pref_completed)

        # ステップ10: 優先順位を付与
        df_with_priority = assign_priority(df_with_survey, survey_emails)

        # ステップ11: 電話番号・郵便番号の整形と住所の作成
        df_formatted = format_contact_info(df_with_priority)

        # ステップ11.5: 住所の標準化
        df_final = standardize_addresses(df_formatted)

        # ステップ12: Excel出力（5シート構成）
        create_excel_output(df_final, regional_stats, df_survey_details)

        # 処理サマリーを表示
        print("\n" + "="*70)
        print("✅ 処理完了！")
        print("="*70)

        print(f"\n【処理結果サマリー】")
        print(f"  📊 最終顧客数: {len(df_final):,} 件")

        # 都道府県データの補完効果
        pref_count = df_final['都道府県'].notna().sum()
        pref_rate = pref_count / len(df_final) * 100
        print(f"  🗺️  都道府県データ: {pref_count:,} 件 ({pref_rate:.1f}%)")

        # アンケート回答者数
        survey_count = df_final['アンケート回答'].sum()
        survey_rate = survey_count / len(df_final) * 100
        print(f"  📋 アンケート回答者: {survey_count:,} 人 ({survey_rate:.1f}%)")

        # 優先順位別の人数
        priority_counts = df_final['優先順位'].value_counts().sort_index()
        print(f"\n【優先順位別】")
        print(f"  優先順位1（12期生）: {priority_counts.get(1, 0):,} 人")
        print(f"  優先順位2（7～11期+回答）: {priority_counts.get(2, 0):,} 人")
        print(f"  優先順位3（7～11期+未回答）: {priority_counts.get(3, 0):,} 人")
        print(f"  優先順位4（1～6期）: {priority_counts.get(4, 0):,} 人")

        print(f"\n  📁 出力ファイル: {OUTPUT_FILE}")
        print("="*70)

    except FileNotFoundError as e:
        # ファイルが見つからない場合のエラーメッセージ
        print(f"\n❌ エラー: ファイルが見つかりません - {e}")
        print("\n以下を確認してください:")
        print("  1. customer_list/ ディレクトリに各期のCSVファイルが存在するか")
        print("  2. surveys/ ディレクトリにアンケートファイルが存在するか")

    except Exception as e:
        # その他のエラー
        print(f"\n❌ エラーが発生しました: {e}")
        import traceback
        traceback.print_exc()


# =========================================================================
# 【エントリーポイント】
# =========================================================================

if __name__ == '__main__':
    # このファイルが直接実行された場合のみ、main() を実行
    # python 01_main_priority.py で実行された場合に動作
    main()
